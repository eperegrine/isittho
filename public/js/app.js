const dodieYellow = "#fef65b"

$(document).ready(function () {
    console.log("ready");

    var app = new Vue({
        el: '#app',
        data: {
            message: 'Hello Vue!'
        }
    });
});